<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% tz = ActiveSupport::TimeZone.all.map.with_index { |x, i| [x.to_s, i+1] } %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :time_zone %><br>
    <%#= select_tag(:time_zone, options_for_select(tz)) %>
    <%= f.time_zone_select :time_zone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
